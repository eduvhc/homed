# SOPS in-place: secrets/*.env vivem encriptados no git (#ENC[...] body).
# Decifrados in-place pelo `task secrets:decrypt` e pelo doco-cd via SOPS_AGE_KEY_FILE.
# Nada de plaintext aqui — qualquer `.env.local`/`.env.tmp` é defensivo.
*.env.local
*.env.tmp
